# Optional secure Java execution service. Without these values JavaShare uses
# the built-in demo result so the interface remains testable locally.
JUDGE0_URL=https://your-judge0-server.example.com
JUDGE0_API_KEY=

